#include<stdio.h>

int main(){
    int radius=2;
    float pi=3.1416;
    printf("the area of the circle is %f",pi*radius*radius);
    int height = 2;
    printf("volume of the circle is %f",pi*radius*radius*height);
    return 0;
}